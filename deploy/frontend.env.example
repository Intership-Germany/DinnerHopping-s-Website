# Frontend build-time environment baked into the static image.
# Copy this file to deploy/frontend.env and edit values per environment.

# Base URL the frontend will use to call backend APIs. In production this is an absolute https URL.
BACKEND_BASE=https://dinnerhoppings.acrevon.fr/api

# Optional: build-time flag to show debug banner
DEBUG_BANNER=false
