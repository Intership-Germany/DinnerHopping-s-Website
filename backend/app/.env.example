# DinnerHopping Backend - Example Environment Variables
# Copy this file to `.env` (DO NOT COMMIT REAL SECRETS) and adjust values.

# --- Core Application ---
MONGO_URI=mongodb://mongo:27017/dinnerhopping
MONGO_DB=dinnerhopping
JWT_SECRET=change-me
JWT_ISSUER=dinnerhopping.local
ACCESS_TOKEN_MINUTES=15
BACKEND_BASE_URL=http://localhost:8000
ALLOWED_ORIGINS=http://localhost:3000
CORS_ALLOW_CREDENTIALS=true

# Cookie/security toggles
# In local dev over http, you may disable secure cookies. DO NOT disable in prod.
ALLOW_INSECURE_COOKIES=false
USE_HOST_PREFIX_COOKIES=true
CSRF_ENFORCE=true
ENFORCE_HTTPS=false
HSTS_PRELOAD=false

# Refresh token lifetime (days)
REFRESH_TOKEN_DAYS=30

# Token & address hashing
# TOKEN_PEPPER should be a long random string; used to HMAC-hash tokens before storing server-side
TOKEN_PEPPER=dev-change-me
# ADDRESS_KEY is base64-encoded key for AES-GCM (16/24/32 bytes). If unset, addresses are stored in plaintext (dev only)
# ADDRESS_KEY=

# --- Stripe (optional) ---
STRIPE_API_KEY=
STRIPE_WEBHOOK_SECRET=

# --- PayPal (optional) ---
# Set these to enable PayPal Checkout (orders v2)
PAYPAL_ENV=sandbox # or 'live'
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=

# --- Wero (SEPA transfer) ---
# If users can pay by bank transfer, configure beneficiary details
WERO_IBAN=
WERO_BIC=
WERO_BENEFICIARY=DinnerHopping
WERO_PURPOSE_PREFIX=DH

# --- SMTP / Email (optional for real email sending) ---
# If SMTP_HOST is unset the backend prints verification & invitation links to logs.
SMTP_HOST=
# 587 for STARTTLS, 465 for implicit TLS (SSL)
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM_ADDRESS=info@example.com
# Whether to issue STARTTLS on non-465 ports (true/false)
SMTP_USE_TLS=true
# Timeout (seconds) for SMTP connect/login/send
SMTP_TIMEOUT_SECONDS=10
# Max retry attempts on transient send failures
SMTP_MAX_RETRIES=2

# Email verification token lifetime (hours)
EMAIL_VERIFICATION_EXPIRES_HOURS=24

